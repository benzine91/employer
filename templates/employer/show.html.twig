{% extends 'base.html.twig' %}

{% block title %}Employer{% endblock %}

{% block body %}
    <h1>Employer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employer.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ employer.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ employer.nom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ employer.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ employer.email }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ employer.adresse }}</td>
            </tr>
            <tr>
                <th>Poste</th>
                <td>{{ employer.poste }}</td>
            </tr>
            <tr>
                <th>Salaire</th>
                <td>{{ employer.salaire }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ employer.dateDeNaissance ? employer.dateDeNaissance|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employer_index') }}">back to list</a>

    <a href="{{ path('app_employer_edit', {'id': employer.id}) }}">edit</a>

    {{ include('employer/_delete_form.html.twig') }}
{% endblock %}
